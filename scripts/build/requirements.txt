# Requirements for build-interactive.py
# Python dependencies for the interactive build manager

# HTTP requests for GitHub API (if needed in future)
requests>=2.31.0

# Note: This script primarily uses subprocess to run shell scripts
# The requests library is optional and only needed if you plan to extend
# the script to interact with GitHub API directly

